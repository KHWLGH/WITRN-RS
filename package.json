{
  "name": "witrn-rs",
  "version": "0.1.4",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "typecheck": "tsc --noEmit -p jsconfig.json",
    "lint": "biome check --error-on-warnings .",
    "format": "biome format --write ."
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.4",
    "typescript": "^5.8.2"
  }
}
